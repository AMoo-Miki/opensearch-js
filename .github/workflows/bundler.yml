name: Bundler

# trigger on every commit push and PR for all branches
on:
  push:
    branches: ['**']
    paths-ignore:
      - '**/*.md'
  pull_request:
    branches: ['**']
    paths-ignore:
      - '**/*.md'

jobs:
  bundler-support:
    name: Bundler support
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Configure sysctl limits
        run: |
          sudo swapoff -a
          sudo sysctl -w vm.swappiness=1
          sudo sysctl -w fs.file-max=262144
          sudo sysctl -w vm.max_map_count=262144

      - name: Runs OpenSearch cluster
        run: |
          make cluster.clean cluster.opensearch.build cluster.opensearch.start

      - name: Use Node.js 14.x
        uses: actions/setup-node@v1
        with:
          node-version: 14.x

      - name: Setup Yarn
        run: |
          npm uninstall -g yarn
          npm i -g yarn@1.22.10

      - name: Install
        run: |
          yarn install
          yarn install --prefix test/bundlers/parcel-test
          yarn install --prefix test/bundlers/rollup-test
          yarn install --prefix test/bundlers/webpack-test

      - name: Build
        run: |
          yarn build --prefix test/bundlers/parcel-test
          yarn build --prefix test/bundlers/rollup-test
          yarn build --prefix test/bundlers/webpack-test

      - name: Run bundle
        run: |
          yarn start --prefix test/bundlers/parcel-test
          yarn start --prefix test/bundlers/rollup-test
          yarn start --prefix test/bundlers/webpack-test
